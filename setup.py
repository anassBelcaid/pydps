from setuptools import setup
setup(name = 'pydps',
version = "0.1",
description =" Discontinuity Position Sweep",
url =  " after",
author =  "A.Belcaid",
licence ='MTI',
packages  = ["pydps"],
# install_requires = ["packages"]
zip_safe = False,
include_package_data=True
)

